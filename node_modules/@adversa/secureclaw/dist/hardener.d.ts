import type { HardenOptions, HardeningResult } from './types.js';
/**
 * List available backups, sorted newest first.
 */
export declare function listBackups(stateDir: string): Promise<string[]>;
/**
 * Run all hardening modules in priority order.
 */
export declare function harden(options?: HardenOptions): Promise<{
    backupDir: string;
    results: HardeningResult[];
}>;
/**
 * Rollback to a previous backup.
 */
export declare function rollback(stateDir: string, timestamp?: string): Promise<void>;
//# sourceMappingURL=hardener.d.ts.map