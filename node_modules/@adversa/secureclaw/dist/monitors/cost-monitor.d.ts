import type { Monitor, CostEntry, CostReport } from '../types.js';
/**
 * Parse a JSONL session log and extract cost entries.
 */
export declare function parseSessionLog(content: string): CostEntry[];
/**
 * Calculate cost for a time window.
 */
export declare function calculateCostForWindow(entries: CostEntry[], windowMs: number): number;
/**
 * Generate a cost report from accumulated entries.
 */
export declare function generateCostReport(entries: CostEntry[]): CostReport;
/**
 * Check spending limits and emit alerts.
 */
export declare function checkLimits(entries: CostEntry[]): void;
export declare const costMonitor: Monitor;
/**
 * Add cost entries externally (e.g., from session hooks).
 */
export declare function addCostEntries(entries: CostEntry[]): void;
/**
 * Set spending limits programmatically.
 */
export declare function setLimits(limits: {
    hourly?: number;
    daily?: number;
    monthly?: number;
    circuitBreaker?: boolean;
}): void;
/**
 * Get the circuit breaker status.
 */
export declare function isCircuitBreakerTripped(): boolean;
/**
 * Reset the circuit breaker.
 */
export declare function resetCircuitBreaker(): void;
/**
 * Reset all state (for testing).
 */
export declare function resetCostMonitor(): void;
//# sourceMappingURL=cost-monitor.d.ts.map