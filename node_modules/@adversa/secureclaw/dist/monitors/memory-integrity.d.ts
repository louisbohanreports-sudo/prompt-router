import type { Monitor, HashBaseline } from '../types.js';
/**
 * Scan a file's content for prompt injection patterns.
 */
export declare function scanForPromptInjection(content: string): string[];
/**
 * Create a baseline of memory file hashes.
 */
export declare function createMemoryBaseline(stateDir: string): Promise<HashBaseline>;
export declare const memoryIntegrityMonitor: Monitor;
/**
 * Reset monitor state (for testing).
 */
export declare function resetMemoryIntegrityMonitor(): void;
/**
 * Get the current baseline (for testing).
 */
export declare function getBaseline(): HashBaseline | null;
/**
 * Set baseline externally (for testing).
 */
export declare function setBaseline(b: HashBaseline): void;
//# sourceMappingURL=memory-integrity.d.ts.map