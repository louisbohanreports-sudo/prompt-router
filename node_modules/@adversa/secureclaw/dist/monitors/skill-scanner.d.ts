import type { SkillScanResult, IOCDatabase } from '../types.js';
/**
 * Scan a single file's content for dangerous patterns.
 */
export declare function scanContent(content: string): Array<{
    pattern: string;
    description: string;
    severity: string;
}>;
/**
 * Scan a skill directory for malicious patterns.
 * This is the main entry point for skill scanning.
 */
export declare function scanSkill(skillDir: string, skillName: string): Promise<SkillScanResult>;
/**
 * Scan a skill from its content directly (for testing without filesystem).
 */
export declare function scanSkillContent(skillName: string, files: Record<string, string>, db?: IOCDatabase): SkillScanResult;
//# sourceMappingURL=skill-scanner.d.ts.map