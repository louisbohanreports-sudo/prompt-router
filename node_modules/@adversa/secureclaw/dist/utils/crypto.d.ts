/**
 * Get a machine-specific identifier for key derivation.
 * Tries /etc/machine-id (Linux), IOPlatformUUID (macOS), or falls back to hostname.
 */
export declare function getMachineId(): Promise<string>;
/**
 * Derive a machine-local encryption key from machine ID and state directory path.
 */
export declare function deriveKey(machineId: string, stateDir: string, salt: Buffer): Buffer;
/**
 * Encrypt data using AES-256-GCM.
 * Returns a buffer containing: salt (32) + iv (16) + authTag (16) + ciphertext
 */
export declare function encrypt(plaintext: string, machineId: string, stateDir: string): Buffer;
/**
 * Decrypt data encrypted with encrypt().
 * Throws if data has been tampered with (GCM auth tag verification fails).
 */
export declare function decrypt(data: Buffer, machineId: string, stateDir: string): string;
/**
 * Ensure the keystore directory exists and write or read the key material.
 */
export declare function ensureKeystore(stateDir: string): Promise<{
    machineId: string;
    keystorePath: string;
}>;
/**
 * Encrypt a file in place, creating a backup first.
 */
export declare function encryptFile(filePath: string, machineId: string, stateDir: string, backupDir: string): Promise<void>;
/**
 * Decrypt a .enc file and return its content.
 */
export declare function decryptFile(filePath: string, machineId: string, stateDir: string): Promise<string>;
/**
 * Generate a cryptographically strong random token.
 */
export declare function generateToken(length?: number): string;
//# sourceMappingURL=crypto.d.ts.map