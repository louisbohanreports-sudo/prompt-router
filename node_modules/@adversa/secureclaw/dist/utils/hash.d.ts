import type { HashBaseline, BaselineComparison } from '../types.js';
/**
 * Compute SHA-256 hash of a file.
 */
export declare function hashFile(filePath: string): Promise<string>;
/**
 * Compute SHA-256 hash of a string.
 */
export declare function hashString(content: string): string;
/**
 * Hash all files in a directory recursively, returning a map of relative path → hash.
 */
export declare function hashDirectory(dirPath: string): Promise<Record<string, string>>;
/**
 * Create a hash baseline for a directory.
 */
export declare function createBaseline(dirPath: string): Promise<HashBaseline>;
/**
 * Compare current directory state against a stored baseline.
 * Returns lists of added, modified, and removed files.
 */
export declare function compareBaseline(baseline: HashBaseline, current: Record<string, string>): BaselineComparison;
/**
 * Save a baseline to a JSON file.
 */
export declare function saveBaseline(baseline: HashBaseline, filePath: string): Promise<void>;
/**
 * Load a baseline from a JSON file.
 */
export declare function loadBaseline(filePath: string): Promise<HashBaseline | null>;
//# sourceMappingURL=hash.d.ts.map