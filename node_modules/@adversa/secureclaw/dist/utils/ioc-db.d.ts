import type { IOCDatabase } from '../types.js';
/**
 * Load the IOC database from the bundled indicators.json file.
 */
export declare function loadIOCDatabase(customPath?: string): Promise<IOCDatabase>;
/**
 * Load IOC database from a raw object (for testing).
 */
export declare function loadIOCDatabaseFromObject(db: IOCDatabase): void;
/**
 * Clear the cached IOC database.
 */
export declare function clearIOCCache(): void;
/**
 * Check if an IP address is a known C2 server.
 */
export declare function isKnownC2(db: IOCDatabase, ip: string): boolean;
/**
 * Check if a domain is known to be malicious.
 */
export declare function isKnownMaliciousDomain(db: IOCDatabase, domain: string): boolean;
/**
 * Check if a file hash matches a known malicious hash.
 * Returns the campaign name if matched, null otherwise.
 */
export declare function isKnownMaliciousHash(db: IOCDatabase, sha256: string): string | null;
/**
 * Check if a skill name matches any known typosquat patterns.
 */
export declare function matchesTyposquat(db: IOCDatabase, name: string): boolean;
/**
 * Check if content matches any dangerous prerequisite patterns.
 */
export declare function matchesDangerousPattern(db: IOCDatabase, content: string): string[];
/**
 * Get infostealer artifact paths for the current platform.
 */
export declare function getInfostealerArtifacts(db: IOCDatabase, platform: string): string[];
//# sourceMappingURL=ioc-db.d.ts.map